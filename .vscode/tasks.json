{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Java",
            "type": "shell",
            "command": "cd \"${fileDirname}\" && javac \"${file}\" && java ${fileBasenameNoExtension} < input.txt > output.txt",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
